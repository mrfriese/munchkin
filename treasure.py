class treasures:

    def __init__(self):
        self.headgear = [None]
        self.armour = [None]
        self.foorgear =[None]
        self.arm = [None]

    def Bad_ass_bandana(self):
        headgear = headgear

    # come back to after basic monster combat is in place.
    # also needs a place to hold or apply the treasure to characters

    # scratch file - create JSON for items and monsters